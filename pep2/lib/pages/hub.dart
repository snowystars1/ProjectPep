import 'package:flutter/material.dart';
class hub extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        padding: EdgeInsets.all(25.0),
        child: Text("hub", style: TextStyle(fontSize:36.0 ))

    );
  }
}
